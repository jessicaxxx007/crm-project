1.用户登录
  接收客户端传过来的两个参数（用户名和密码）
  非空检验，判断用户名和密码是否为空
  通过用户名找到对应的用户信息，也就是一个用户对象
  判断客户端输入的密码和用户对象数据库的密码是否一致
  如果一致 登录成功

 controller层 控制（接收请求，响应结果）
 1.形参去接受客户端传过来的参数
 2.将参数转发到业务逻辑层的login方法（响应登录结果）
 3.将结果封装返回给客户端

 service（业务逻辑层：非空检验，条件校验等业务逻辑操作）
 1.对用户名和密码进行非空检验
     如果用户名和密码为空：则抛出异常，控制层接受捕获
 2.调用持久层通过用户名找对应的用户信息，也就是一个用户对象
      如果说用户对象为空：抛出异常，控制层进行捕获
 3.判断客户端输入的密码和用户对象数据库的密码是否一致
       如果不一致：则抛出一个异常，控制层直接捕获

 Dao（持久层 sql增删改查操作）
 通过用户名去数据库找到我们的对应用户信息